{
  "$id": "gts://gts.frontx.uikit.vocabulary.coverage.v1~",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UiKit coverage",
  "description": "What this contract claims about itself: a decision per claim, plus the claims nothing could verify. Open by construction - the claim names are the kit's own and grow with it - which is why the decision type is referenced from additionalProperties rather than from a fixed property list.",
  "type": "object",
  "properties": {
    "assumptions": {
      "type": "array",
      "items": {
        "$ref": "gts://gts.frontx.uikit.vocabulary.coverage_assumption.v1~"
      }
    }
  },
  "additionalProperties": {
    "$ref": "gts://gts.frontx.uikit.vocabulary.coverage_outcome.v1~"
  }
}
