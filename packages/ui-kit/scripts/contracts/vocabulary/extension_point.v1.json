{
  "$id": "gts://gts.frontx.uikit.vocabulary.extension_point.v1~",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "UiKit extension point",
  "description": "One growth point a consumer extends this component through, declared at the component level instead of enumerating every plugin-shaped prop individually (DataTable's `columns` accepts arbitrary third-party ColumnDefs, whose own render functions are opaque to a JSON Schema extractor regardless of how many are listed).",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1
    },
    "kind": {
      "type": "string",
      "enum": [
        "prop",
        "helper",
        "feature"
      ]
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "typed_by": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "name",
    "kind",
    "description",
    "typed_by"
  ],
  "additionalProperties": false
}
