# Fixture Shell

A minimal, self-contained synthetic template used only by the CLI's own test
suite (`packages/cli/src/__tests__/`). It is not a real FrontX template and
ships no real product code — it exists solely to exercise the install/seed
pipeline against a tiny, self-contained "shell"-shaped template.
