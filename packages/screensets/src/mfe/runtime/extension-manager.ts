/**
 * Extension Manager state types and abstract interface — re-exported from @gears-frontx/mfes
 * (Phase 7 extraction). Retained for local-import backward compatibility.
 */
export {
  ExtensionManager,
  type ExtensionDomainState,
  type ExtensionState,
  type LifecycleTriggerCallback,
  type DomainLifecycleTriggerCallback,
} from '@gears-frontx/mfes';
